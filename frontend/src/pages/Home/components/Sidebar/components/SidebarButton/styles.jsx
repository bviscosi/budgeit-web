const styles = (tab) => ({
	iconStyles: {
		padding: '0.5rem',
		display: 'flex',
		backgroundColor: tab === 'account' ? '#272727' : 'transparent',
		borderRadius: '100%',
	},
});

export default styles;
